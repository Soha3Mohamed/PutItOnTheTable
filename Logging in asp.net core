
الـ **logging** من الكونسبتس اللي كنت عارفه إنها مهمة جدًا، وعشان كده فضلت مأجل مذاكرتها لحد ما أركز فيها كويس. بقالي يومين بقرأ عنها وأطبق عملي، وقلت أشارك الصورة الكاملة.

الـ logging ببساطة يعني إنك توثّق كل اللي بيحصل في الأبلكيشن بتاعك من أول الـ request لحد الـ response. الهدف إنك تلاقي عندك بيانات ترجع لها لما يحصل bug مش مفهوم أو لما تحب تتابع رحلة الريكويست من أوله لآخره.
فكّر فيها كإنك بتسجل فيديو للمشوار كله 🎥.

###  مستويات الـ Logs

`Trace`, `Debug`, `Information`, `Warning`, `Error`, `Critical`

* `Trace` →  Tiny Details.
* `Debug` →  Developer Details.
* `Information` → Normal Events.
* `Warning` → Clear.
* `Error` → There is a problem but the application is running.
* `Critical` → The application may crash.

لما تختار **MinimumLevel** معين، بتسجل المستوى ده وكل المستويات الأعلى منه (في الشدة).

###  ASP.NET Core والـ ILogger

.NET Core فيها built-in logging عن طريق `Microsoft.Extensions.Logging`، ومن خلال `ILogger<T>` تقدر تكتب لوجز بالشكل ده:


_logger.LogWarning("User with email {Email} already exists.", user.Email);


الـ `ILogger<T>` ده مجرد abstraction، وورا الكواليس ممكن يشتغل بأي provider تختاره.

###  ليه أستخدم Serilog؟

* **Sinks**: أقدر أكتب الـ logs في أكتر من مكان (Console, File, Database, Remote Service زي Seq أو Kibana).
* **Structured Logging**: اللوجز بدل ما تكون نص عادي تبقى JSON → تقدر تعمل queries & search على أي property زي `UserId`, `Duration`, أو `Url`.
* **Enrichers**: تضيف أوتوماتيك metadata زي `RequestId`, `MachineName`, ومعلومات تانية.
* **Destructuring**: لو عملت log ل object كامل Serilog بيحوّله JSON منظم بدل ما يطبع `ToString()`.

مثال بسيط:


Log.Logger = new LoggerConfiguration()
    .Enrich.FromLogContext()
    .WriteTo.Console(new Serilog.Formatting.Json.JsonFormatter())
    .WriteTo.File(new Serilog.Formatting.Json.JsonFormatter(), "logs/log.json", rollingInterval: RollingInterval.Day)
    .CreateLogger();


المثال ده بيخلي الـ logs تطلع structured JSON سواء في الـ console أو في ملفات يومية.

###  ملاحظات مهمة

* مهم متسجّلش بيانات حساسة زي Passwords أو معلومات بطاقات.
* لازم تقرر لحجم اللوجز (Retention & Rolling files) عشان ما تملأش السيرفر.
* في production يفضل تعمل الـ config من `appsettings.json` عشان أي تعديل يتم من غير إعادة build.

---
المرة الجاية هتكلم عن seq ودي UI Tool for Logs Management

